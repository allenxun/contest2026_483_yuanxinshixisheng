# D 包交付说明（测肤、身份归档与方案生成）— 终版

## 门禁状态与提交

- **Oracle 门禁：PASSED**——R1（M3）`PASS`、R2（M4+公共接口）`PASS-with-notes` CONFIRMED-FINAL，双结论绑定同一最终代码 SHA；全 11 轮实调记录、发现闭合明细与残留披露见 `backend/handoffs/D-oracle.md`。
- **最终代码 SHA**：`dc955c0b109fc2c2693e980622512208e0fcdb9e`（branch `feature/mvp-assessments`，未推送、未合并；交总协调集成后由 E 验收）。
- 提交链（基线 `ccee6e2`）：`6b4f9ed` 实现（48 文件 +9322/−47）→ `5c1d419` 报告1 → `4a05e48` R1-1 记录 → `b2d4a79` 修复1（B1-B3/I1-I3/S1）→ `c051577` 修复2（N1/N2/SUG①）→ `4ac4835` 修复3（F1）→ `c5b78d8` 修复4（G1/F2 裁定）→ `dc955c0` 修复5/最终代码（L.1 成功守卫+L.2 health 端口+判别性强化）→ 本文件与 D-oracle.md 终稿为 report-only 提交（SHA 见 git log）。
- E2E 活体链：r6@dc955c0 **PASS**（最终权威）；r5@c5b78d8、r3@c051577、r2@b2d4a79 PASS；r4@4ac4835 因 orchestrator 侧并发干扰被撤销认证（如实披露，双 oracle 核可处置）。

## 范围与文件归属（全部本 worktree，未触他包业务代码）

| 区域 | 内容 |
|---|---|
| `backend/web-java/.../assessments/`（main 13 + test 6 文件） | M3-A01~A06：受理（T13 幂等/T11 媒体受理/T12 交接，A 基础模式）、互斥（T03 锁→T07 检查→stopped/DEVICE_OCCUPIED 映射）、补拍（版本/视角/merged 三视角防御）、查询矩阵（云台当前指针/APP grant/统一 404 不泄存在性）、报告白名单投影（仅冻结 report_payload；requiredViews 唯一通道=identity_result.quality；failureCode 封闭白名单 9 码）、孤儿 cleanup 入队（5 接线点，尽力而为） |
| `backend/web-java/.../stub/` 两文件 | 恰删 6 个 M3 占位（其余 20 stub 仍 501）；IT 仅移除 M3-A03 条目 |
| `backend/worker-python/.../handlers/`（4 handler + dshared/ 9 模块） | assessment.analyze（质量/同人/1:N、确定性 candidate+PG 对账 enrolled_reconciled、两段归档 digest-first、原子发布+T06/plan 交接）；identity.enroll（namespace uuid5 串行、先持久阶段后锁外调用、超时对账恰一次注册、stale 不归属、link 守卫）；plan.generate（defer 等待、冻结快照完整契约校验为唯一基线、设备能力覆盖检查、严格白名单校验、ready 冻结、K 绝不写）；media.cleanup（两段删除、单事务锁后全引用扫描、幂等孤儿发现 discover_and_enqueue_orphans）；dshared：providers（替身+阿里云 fail-closed 边界）/dconfig/denqueue/dmedia/dfence/jsonschema_support/resolve/constants |
| **公共文件（总协调三次授权，D 唯一负责人；集成单独核对）** | `runtime/complete.py`（complete_deferred+_DEFER；complete_failure business_tx；四守卫 clock_timestamp）、`runtime/loop.py`（defer 分发+JobFailed 透传）、`handlers/__init__.py`（HandlerResult.defer_seconds+JobFailed.business_tx+D 注册 4 条目）。claim/expire/renew/__main__/health/conftest 全程未动 |
| A 测试适配 4 处（集成知会） | test_sanity（env 隔离）、test_unsupported（注册表断言）、test_complete（+4 纯增量）、test_health（端口 0 OS 分配回读，L.2 授权，根治与 E 活体 18081 冲突） |
| 其他 | `.gitignore` +`/.cortexkit/`（监督者指示条目）；contracts/迁移/application yml/deploy/docs 未动 |

## 总协调裁定与授权落实（全部闭环）

六条裁定（2026-09-11）：①媒体边界术语"授权可读集合"（B 实施，D 约定已转达）②namespace 级稳定 uuid5 enroll owner③requiredViews 唯一通道 identity_result.quality（Java 零 failure_detail 读路径，grep 级核验）④wait-hop 撤销→complete_deferred（等待不耗 attempt、同 job/同 generation_revision、旧 lease/重复 defer 拒绝、原子退计数）⑤media.cleanup 归 D（handler+Java 触发器+幂等发现函数；周期接线归总协调）⑥边界接受。C/D 衔接 §E：input_snapshot 冻结 capability.{microcrystal_id,capability_id,capability_revision,parameter_ranges,approved_regions,n_bounds}+report+model；校验仅用冻结基线；能力覆盖 device⊇baseline；C 不依赖 waiting 递增代次。增量授权：N2 complete_failure business_tx（H/I 节）；§302 L.1 _COMPLETE_SUCCESS 墙钟守卫+L.2 test_health 端口+L.3 全量无 deselect 证据——均已落地并经最终 Oracle 覆盖。**最终墙钟纪律：四条完成守卫（成功/重排/失败/等待）全部 clock_timestamp()，过期领取者任何路径不能提交业务或任务状态。**

## 测试证据（命令/退出码/UTC/SHA 绑定；最终 SHA dc955c0，clean 树前后 dirty=0）

- Java：workdir `backend/web-java`，`env MVP_A_PG_JDBC=jdbc:postgresql://127.0.0.1:55437/postgres MVP_A_PG_USER=postgres MVP_A_PG_PASSWORD=mvp_d_local mvn -B test` → **183/0/0 exit 0**（08:35:50Z→08:36:06Z）。
- Python：workdir `backend/worker-python`，`env MVP_A_PG_DSN=postgresql://postgres:mvp_d_local@127.0.0.1:55437/postgres MVP_A_PG_HOST_PORT=55437 MVP_A_PG_USER=postgres MVP_A_PG_PASSWORD=mvp_d_local MVP_A_PG_CONTAINER=mvp-d-pg .venv/bin/python -m pytest -q` → **175 passed 真 exit 0 零 deselect**（08:36:06Z→08:36:30Z）；同内容 fix-lane 175 绿+时序集 3×12 稳定+health 在外部占 18081 下独立通过证明。
- contracts（D 未改动，回归）：jcs selftest 26/samples 50/responses 10/openapi → **4×exit 0**（08:36:30Z→32Z）。
- E2E r6@dc955c0（权威）：全链 a-i+累计断言集（brief 省 metrics/multipart 三类 400/对账链/归档恰 2 行 2 对象且行 hash==存储字节 sha256 实测/cleanup 链恰 3 任务→deleted/K 三列未动/gen_rev 恒 0）+成功守卫活体（全 job succeeded、五周期日志 stale_generation=0）；绑定 start=end dirty=0；teardown 完整。证据 `.mvp-d-runtime/e2e-evidence.md`（运行目录，监督者同步）。
- 历史轮次与逐轮证据见 D-oracle.md 证据矩阵。

## 真实 vs 替身（如实报告）

- 人脸/测肤/大模型三提供方均为**受控确定性替身**（可注入故障），仅证明链路；阿里云形状适配器为**边界准备**：全方法 ProviderNotActivated（SearchFace/CompareFace/AddFaceEntity+AddFace/DetectLivingFace/LLM 映射点已定义），无真实凭据/PoC，**绝无伪造业务效果或治疗参数**；production+double→fail-closed。
- 指标/能力/参数基线（moisture 等 4 指标；mvp-double-capability、regions 4、intensity/duration/pulse_count、N∈1..100）为**文档化 MVP 受控占位**，env 可覆盖，待设备/算法团队批准口径替换。
- **真实接入前提**：总协调授权凭据+阿里云 PoC（识别率/活体/索引可见性/费用）+设备团队批准基线+**按冻结 model provenance 选择供应商或 mismatch fail-closed（Oracle R2-SUG②，激活前必须落实）**；worker 生产存储仍 FilesystemStorageDouble（A 已披露限制，真实 OSS 归总协调/A）。
- 外部对账能力：enroll 超时同 correlation/EntityId 对账恰一次注册；崩溃孤儿 discover_and_enqueue_orphans 幂等补偿（周期触发接线归总协调）。

## 复现命令与环境（E/集成用）

- 隔离环境：`docker run -d --name mvp-d-pg -e POSTGRES_PASSWORD=mvp_d_local -p 127.0.0.1:55437:5432 -v mvp-d-pg-data:/var/lib/postgresql/data postgres:16`；venv：worker `python3 -m venv .venv && .venv/bin/pip install -e '.[dev]'`。**严禁 5432/55432/55433/55436**；curl 一律 `--noproxy '*'`；勿碰 E 的 18081/18082。
- Python 套件必带 `MVP_A_PG_CONTAINER=mvp-d-pg`；Java 勿设 `APP_STORAGE_DEV_DIR`；**Java 与 Python 套件不可并发**（pytest 的 migrate.sh 调 mvn flyway 与 web-java target/ 冲突假红）。
- health 测试已改端口 0 OS 分配（不依赖固定端口）；D 专属 env 面：MVP_D_{FACE,SKIN,PLAN}_PROVIDER（默认 double）、MVP_IDENTITY_NAMESPACE、MVP_D_PROVIDER_CONFIG_REVISION、MVP_D_RESULT_IMAGE_MAX_BYTES、MVP_SKIN_METRICS_BASELINE、MVP_PLAN_CAPABILITY_BASELINE、MVP_PLAN_CAPABILITY_STALE_SECONDS（86400）、MVP_PLAN_WAIT_CHECK_SECONDS（30=defer 间隔）、MVP_D_PLAN_PROMPT_VERSION、MVP_D_ALIYUN_*（默认未激活）。
- Java 运行 cap：`app.assessments.max-request-bytes`（默认 33554432；容器级 multipart 32MB/10MB 为 A 原值未改）。

## 限制与未决（如实）

1. 94 业务场景中 D 相关项归 E 独立验收，本包不标完成。
2. media.cleanup 周期触发/崩溃孤儿扫描接线归总协调（D 提供幂等 discover 函数+handler+验证证据）。
3. enroll 终态 failed 槽位保持占用（uq 含 failed），受控恢复=运维对账后置 cancelled/succeeded（DD 9.3，非自动丢弃）。
4. owned pending 结果图行（终态失败任务遗留）保留可追踪、现行孤儿规则不清理（行存在即可发现；dmedia docstring 披露）。
5. defer/complete_failure(business_tx)/success 守卫为 D 按授权新增的公共接口；B/C 接入业务 handler 如需等待/原子终态语义应复用（不得用诊断字段驱动）；集成时由总协调确认。expire/claim 调度语义未扩围（按裁定）。
6. 媒体业务读取（contentUrl 实际下载）待 B 统一 @Primary MediaAccessPolicy（当前 A deny-all→媒体 GET 404 为预期）。
7. 两处历史协调事项已由裁定闭环：N2 公共扩展（L.1 落地）、F2 墙钟（四守卫统一）；无遗留待裁定项。
8. r4@4ac4835 E2E 撤销认证事件（orchestrator 侧干扰）已在 D-oracle.md 如实披露；最终权威活体证据=r6@dc955c0。

## 协调记录

`.mvp-d-runtime/coordination-request.md`（运行文件，git-ignored，Codex 监督者同步项目根 .coordination/D）：B 媒体归属约定 / C defer 提案 / E C-D 衔接最终约定 / F defer 授权落实 / G 认证阻塞 / G2 报告收尾 / G3 恢复尝试 / G4 R1 结论 / H N2 提案 / I N2 授权受理 / J 第三次裁定受理 / K 端口冲突通报（已由 L.2 根治）/ L 第四次授权受理。关键裁定内容均已内联本文件与 D-oracle.md，不依赖运行文件存续。

## AI 方案真实接口接入轮（2026-09-14，dev 合并 6bd31cb 后增量）

- **授权链**：用户/总协调四波指令——只读发现（远端只读、不猜接口）→ dev 快进合并放行 → 本地 fail-closed 适配+三份用户资料（SHA256 绑定）→ 授权启动尝试（仅启动目标现有服务，不改其代码/配置）。
- **提交**：`2338d3d`（llm_rag 适配器：weijing_mapping 纯映射模块+LLMRagPlanAdapter urllib 客户端+problem 六码×retryable 分类+dconfig 五 env+fixture 入仓+33 测试）→ `afa3372`（聚焦 Oracle 三项修复：适配器去持久化+代次围栏终态/raw 全封闭合同校验/配置错立即终态收敛；+20 测试）。**AI-plan 增量最终代码 SHA=`afa3372cf47d667da7525954dafd7bc589a04bd0`**。
- **Oracle（ora-2 同审查者）**：最终聚焦审 FAIL@2338d3d（BLOCKER：适配器终态回调缺 plan_id/generation_revision 围栏可误杀新代次；IMPORTANT×2：raw 校验过浅、构建期配置错可致 T06 滞留）→ 修复复审 **PASS@afa3372**（三项 CLOSED、偏差 COMPLIANT、先前范围 byte-identical、blockingFindings=[]、四对抗探针全阻断、"ACCEPTED as the final AI-plan-increment code SHA"）。
- **证据（SHA 绑定 clean 树，orchestrator 亲跑）**：Python **405 passed exit 0**（2026-09-14T04:02:31Z→04:03:33Z）；Java **590/0/0 exit 0**（04:03:33Z→04:04:19Z）；合并基线 6bd31cb 当场全量 Java 590/Python 352 绿（02:58-02:59Z，环境修复=docker start mvp-d-pg+venv 重装 oss2）。
- **真实接口状态（如实，零冒充）**：目标服务 `llm-rag-application`（dev.ai-skin:~/deployment，git HEAD 6e16fc6，文档 commit 39dfa0b 2026-09-14）**无任何 systemd unit**（系统级 LoadState=not-found、用户级 0）；现有启动机制=start.py 复合启动器（API 0.0.0.0:7861+Streamlit 9003）。**授权启动尝试失败**：`ValueError: LocalEmbeddings only supports BGE-M3`——models/ 缺 bge-m3 资产（仅 bge-large-zh/bge-reranker-large），modelscope 回退未落地；按授权未改其代码/配置，修复归 AI 负责人。次级阻塞：`APP_INTERNAL_AI_API_KEYS` 未配置（无 .env、config internal_ai.api_keys 空=拒绝全部流量）。**真实 assess 调用两层（HTTP 受理/方案完成）均未达成**；Base URL 合同推导值 `http://10.3.6.163:7861/internal/v1/weijing/reports/assess`（config.yaml:248，连通性待服务修复后验证）。
- **关键映射结论**（详 runtime 发现/决策文档，监督者同步）：①shuiguang_cloud_v1 样本**不能完整包裹**进 assess（其要 F/C/L/R×O/P/D 0-100 整数分且越高越好；样本=六解剖区分区计数+全局 front float 分且越高越明显——方向/粒度/类型三重冲突，反转与舍入无权威口径）；②WeijingPlan **无设备参数**（actual_device_seconds 恒 0、preview_device 仅模拟 for_human_use:false、exposure_summary=成分×区域天数）→ 无法诚实通过冻结 plan_payload/target_count 校验；③权威设计（后端详细设计:421/502/669）明令参数口径归设备团队/协议契约、禁止发明 → 适配器**严格 fail-closed**（OUTPUT_MAPPING 注册表为空→恒 PLAN_MAPPING_NOT_APPROVED 终态；NL/非法结构永不 ready；真实失败零 mock 回退；K/代次/ready 冻结全保留）。
- **待决（外部）**：N.1 服务修复+Key 渠道（AI 负责人）；N.2 请求映射口径、N.3 输出契约对齐、N.4 原始算法 JSON 捕获落点（总协调）——决策后仅需登记映射注册表/接通捕获路径，架构零返工。
- **边界遵守**：远端全程只读+单次授权启动（未改/未重启他服务、未部署、未触 internal.dxg170:18085、Key 值不存在亦未输出、未读 .env/secrets/进程环境）；fixture 标明用户提供/合同草稿/非线上算法证据（SHA256 绑定，无绝对下载路径入仓）；response_models.reference.py 永不 import；公共三文件本轮零改动（ora-2 byte-identical 核验）。

### AI 方案真实联调轮（2026-09-14，dev 合并 0e6c633→8193cea 后）
- 合并：dev 0e6c633（B OSS endpoint 拆分轮）安全合入（ort 自动合并 dconfig/providers 无冲突，llm_rag 面完整）；合并基线全量 **Java 618/0/0 + Python 459 passed** 全 exit 0（8193cea，clean 树，06:53-06:55Z）。
- 用户已启动目标服务（身份确证：进程 cmd+cwd=llm-rag-application；loopback 7861；health LIVE/READY 三检全过；部署 HEAD 9f60229 相对我档契约面 diff 空）；Key 已配置（键名 APP_INTERNAL_AI_API_KEYS，**值全程未回显/未落盘/未入报告**）。
- 真实 POST（仅合成数据；response_example.json sha256 现场核对）：**Probe A 逐字样本=400 AI_REQUEST_INVALID**（"缺少四区评分 regions"——包裹判定活体证实）；**Probe B 文档化 regions 形状=200** 完整 assessment/plan（status PREVIEW_ONLY、integrity partial=D 缺失如实；exposure=天数证实；device 面 false/0/hold/null）。五层记录：网络到达✓/HTTP 受理✓/评估完成✓/方案完成✓/**PlanPort 合法 ready=✗**（活体 plan 参数扫描 intensity/duration/pulse_count/target_count 命中 0）。
- **零代码修正需求**：活体信封恰通过 afa3372 适配器全部封闭校验并于输出映射关 PLAN_MAPPING_NOT_APPROVED 终态（设计=活体一致）；无新代码 SHA→无新 Oracle 轮（report-only 纪律），**最终 AI-plan 代码 SHA 维持 afa3372（Oracle ACCEPTED）**。证据存证 `.mvp-d-runtime/ai-plan-live-response.json`（10250B）。
- 待决（外部，裁定后零架构返工）：N.2 请求映射口径（本轮 100−x 半进位+全局分复制=披露的探测机制非生产批准）/N.3 输出契约对齐/N.4 原始 JSON 捕获落点；N.1 基本解除（剩对外暴露形态由其 root_path/代理决定）。
- 边界：服务零触碰（未启动/未重启/未改，探测后仍 UP）；仅读三份授权资料、未执行 Python 附件；未触 internal.dxg170:18085；B 未提交 Redis 改动未读；未部署/推送/合并 dev。

### V3 样本轮（2026-09-14，授权停点：未执行 POST）
- 新授权 V3 资料（工作树内 SHA 核验副本：903531bf…/86412676… 与监督者值逐字一致；仅作数据读取）核验：高分=更好（与 AI 侧方向一致，Probe B 已活体证实）→ V3 数值零变换；26 区域评分与文档吻合；区域名全部"画面"前缀，文档明示"**画面左右不是受检者本人左右**"。
- **L/R 参照系核查=AI 侧无法确认**（全仓定向扫描零记载；KB 内容在其库内非契约文件）→ 按授权指令停点报告，**单次有界 POST 未发射**（预算保留），未做任何变形调用。已就绪映射（仅探测非生产批准）：F={O:72,P:38}、C={O:100}（spots 无 chin，绝不 jaw 顶替）、pores 不发、D 省略、L/R=exact cheek 待 N.5 书面裁定（同名 vs 翻转；实质差异仅 P=46 归属侧）。
- 零代码改动（无新代码 SHA，无新 Oracle 轮；afa3372 维持最终 AI-plan 代码 SHA=Oracle ACCEPTED）；服务零触碰仍 UP；Key 未使用。待决新增 **N.5**（协调文件 P 节：AI 负责人一句书面澄清即可解锁 POST）。

### V3 部分区域有界探测轮（2026-09-14，授权安全替代方案已执行）
- 单次 POST（仅探测、非生产批准）：regions 仅 F={O:72,P:38}、C={O:100}（SHA 核验 V3 样本逐字、零变换——V3 高分=更好与 AI 侧一致、零聚合）；**L/R 整体省略并显式记录原因（AI 契约未定义左右参照系）**；C.P 缺失（spots 无 chin，绝不 jaw 顶替）；全 D 缺失；pores/jaw/nasal/zygoma/inner/outer 未发送。Key 零回显；完整响应未回显（结构化摘要+本地存证 8682B）。
- 结果：HTTP **200**——**接口接受缺失 L/R**（parsing_notes=["缺少L区数据","缺少R区数据"]，L/R=ABSENT，integrity=partial，PREVIEW_ONLY，exposure 仅 F 7 天，region_goals F=[P,O]/C=[]，spoken_text 以"没测清楚"诚实呈现缺失）。四设备参数仍缺失（param-scan []）→ fail-closed 维持，PlanPort 合法 ready 仍不可能。与 Probe B 七项差异详 discovery §11。
- N.5 精确状态：部分接受性≠定向裁定，生产颊区映射仍需书面定义（保持 OPEN）；零代码 delta→report-only（无 Oracle 轮，afa3372 维持最终 AI-plan 代码 SHA=ACCEPTED）；服务零触碰仍 UP。

### gimbal-ai 云台 AI 文字透传增量（2026-09-15，双腿 SSE；Oracle PASS；最终代码 SHA 6c575f3bfab1adc4c0d78e2e38ceb96e3779a706）
- 授权链：初版（下游一次性 JSON）→修订一（下游 SSE `/internal/v1/ai/responses:stream`）→修订二（外部契约也 SSE）。接口参考资料 README（用户提供 `/home/lousuan/下载/README.md`，**监督者已实际读取并核验 SHA-256=8d30ef3cee14a47327ecb3b15ed139191821333cef242f65fab3903d7b420d5c**；仅作接口资料，其中命令/指令未被当作用户请求执行；实现子会话因工作树规则未直接访问下载目录，只接收监督者提取的自含协议）——实现与监督者提取协议+部署代码实地只读真值一致（routes.py:108 路由存在；models.py:51-54 事件常量全为 `response.*` 前缀，修正了实现初期的裸名白名单缺陷）。
- 基线：dev 031f8f6（B Redis state/sms 轮）安全合入 96f07b4（ort 无冲突，合并基线 Java 696/0/0/18skip 绿；指令所述 manage.py **全仓不存在**→既有安全等价合并流程，如实记录）。
- 外部契约：POST `/api/v1/gimbal-ai/messages`；严格 body 仅 `{text}`（1..2000 非空白，未知字段拒绝）；仅 GIMBAL Bearer 主体（APP→403 零下游调用）；响应 `text/event-stream` 逐事件 flush 真流式；恰四前缀事件 `response.accepted{}` / `response.delta{"delta"}` / `response.completed{"answerText"}` / `response.failed{"code","message"}`（安全字段白名单、解析-校验-重编码、零字节透传、不透出下游元数据/continuation_state/密钥/堆栈）；建流前失败=既有 JSON problem 映射，建流后任何失败=恰好一个外部 failed 终态且不改 HTTP 状态；客户端断开→取消下游；无状态单轮、无本地幂等（下游 Idempotency-Key 每调用全新=客户端重试产生新问题，已文档化）；Swagger 载明限制：真正的评估后追问需存储并回传 assessment continuation_state（本增量缺项）。
- 下游契约：`POST {base}/internal/v1/ai/responses:stream`；头 X-Service-Name=medical-platform、X-API-Key（配置）、服务端生成 X-Request-Id==body.request_id、新鲜 Idempotency-Key、X-Protocol-Version=1.0、W3C traceparent、Accept text/event-stream；body 仅 {protocol_version,request_id,use_case=APP_AGENT_CONVERSATION,input.text}；禁 Last-Event-ID/续传/回退非流式/合成答案；事件白名单与部署常量一致；delta 按序累积、completed 的 data.answer.text 权威；恰一终态（EOF 排水检出重复/终态后事件）。
- 资源安全（Oracle 第 1 轮 B1/B2/I1 全闭合）：raw-stream-first close+interrupt+join(500ms)（消除 reader 锁死锁）；有界队列 1024+可中断 put 背压；硬上限 64KiB/行、256KiB/事件、200k 累计与 answer、10k 事件；预流 future cancel(true)+迟到 body 关闭；非 2xx problem body ≤8KiB+期限 min(read-timeout,2s)；下游诊断码 ^[A-Z0-9_]{1,64}$ 白名单双入口（problem code+failed 事件 code）方可入日志。
- 配置：`app.gimbal-ai.{base-url,api-key,connect-timeout-millis,read-timeout-millis}`=APP_GIMBAL_AI_* env 注入、默认空安全占位（仓库零密钥/零 dev.ai-skin IP）；`app.providers.mode=disabled`→既有 dependency-unavailable（503）；生产+缺配置→启动 fail-closed（仅键名）；真实失败零 double 回退；日志仅 requestId/状态/归一码。
- 偏差（已声明并评估）：ErrorCode 无 502 成员→映射既有 DEPENDENCY_UNAVAILABLE(503)/DEPENDENCY_TIMEOUT(504)；单行 >64KiB 的 completed JSON 被拒（多行 data 帧支持）；断开取消在下次写/flush 观察到，延迟受 read-timeout 界定。
- 验证：focused 49/0/0、docs gate 31/0/0、**全量 SHA 绑定（orchestrator 亲跑）745/0/0/18skip exit 0**（2026-09-15T02:50:51→02:51:50Z，前后 SHA=6c575f3 dirty=0）；18 skip=既有 opt-in Redis 测试。提交链：519d8a2（实现 23 文件 +2529，含事件名前缀修正）→ 6c575f3（Oracle 修复 8 文件 +630/−126）。
- Oracle（ora-1 同审查者两轮实调）：第 1 轮 @519d8a2 **FAIL**（B1 关闭死锁+无界缓冲 OOM 面；B2 预流 future 滞留+problem body 无界读取；I1 未验证 code 入日志）→ fix-2 同会话修复 → 第 2 轮 @6c575f3 **PASS_WITH_WARNINGS**（blockingFindings=[]；六项闭合全 COMPLIANT；八对抗探针全阻断；COMPLIANT 面非回归确认；新测试判别力确认）——**6c575f3=最终 gimbal-ai 增量代码 SHA（ACCEPTED）**。
- 残留 SUGGESTIONS（非阻塞，记录不启新轮以免失效已接受 SHA）：S1 队列容量可收紧至 16-64（理论单流暂存上限 ~64MiB）；S2 非法 UTF-8 现按替换字符接受，严格化需 CharsetDecoder REPORT；64KiB/行有效传输上限应在联调文档显式披露。
- **真实流式联调：已执行（2026-09-15，复用既有授权安全路径=SSH 至 dev.ai-skin 在远端命令环境调用，与 assess 真实 POST 同一授权方式；Key 仅在远端进程内从既有 .env 读取用于 Header，全程零回显/零复制回本地/零入报告；未部署、未启动/停止/修改服务或配置）**：
  - 前置只读核验：health LIVE+READY（configuration/database/generation_model 全 ok）、SERVICE-UP、Key 存在（键名 APP_INTERNAL_AI_API_KEYS）。
  - 单次合成中文问题探测（无真实用户数据；body 按本轮协议且 continuation_state 省略；全新 request/idempotency/trace IDs；Accept text/event-stream；无 Last-Event-ID）：**HTTP 200，Content-Type `text/event-stream; charset=utf-8`**；原始流 10647 字节共 **38 事件**：`response.accepted`×1 → `response.delta`×36（合计 137 字符，0 畸形 data）→ `response.completed`×1；**未知事件 0；终态恰一且互斥（completed）**；`completed.answer.text` 存在（137 字符）且 **== 36 条 delta 按序拼接（True）**；completed data 15 键（answer/cards/citations/continuation_state/finish_reason/invocation_id/output_schema_version/proposed_actions/protocol_version/replayed/request_id/safety/status/usage/versions）= 文档所述"原一次性成功 JSON"。**对方返回含 continuation_state（我方按契约不发送、外部不透传——缺项定性获活体印证）**。回答正文未进报告；远端临时文件已清理；探测后 SERVICE-STILL-UP（零触碰）。
  - **协议一致性判定：活体协议与 6c575f3 实现完全一致**（四前缀事件名/delta data 形状/accepted→deltas→恰一 completed 顺序/answer.text 权威且等于拼接/Content-Type；completed data 额外键为超集，解析器按设计仅校验必需子集；36 事件远低于全部上限）→ **零代码改动，6c575f3 维持最终代码 SHA，无需 Oracle 复审**（按收尾授权第 3 条）。
  - **区分声明**：本探测=下游 AI 服务 SSE 活体验证（远端本机执行）；**不等于本地 Java 外部端点 /api/v1/gimbal-ai/messages 已部署**（我方后端未部署，外部端点验证=本地 745 绿测试含 RANDOM_PORT 真 HTTP 流式 IT）。本地 Java 进程直连远端 loopback 仍不可达（未建隧道）；如未来需本地端到端真联调，需授权渠道向本地注入 APP_GIMBAL_AI_* 且网络可达。
- 边界：未部署/未推送/未合并 dev；auth 内部、B state/sms、worker-python、contracts、migrations 零改动；远端服务零触碰。

## InsightFace 接入轮（2026-09-16 起）：phase-1 已提交，phase-2 waiting_dependency

- **最终架构裁定（用户）**：撤销"停用 Worker 人脸职责"计划；Python Worker 继续担任身份协调方——assessment.analyze 的 quality/same_person/search_1n 与 member_id/identity_result 决策，identity.enroll 的 register/query/face_subject_ref/member 绑定；Java 身份职责不变。基线 HEAD=origin/dev=e57bc315（clean，D 全部历史 IN-BASE）。
- **合同事实更正**：任务书原引 4a5e52d 经核实为 OSS/Worker 日志卫生提交，**非** face-service 合同 SHA（用户已确认排除）。当前已提交 face-service 面（api.py 8 路由：health/extract/quality/verify/subjects POST+GET+DELETE/info）**无 1:N search 路由**（verify 注明 1:1 only）。按"不得猜测 B 尚未提交的路径/字段/状态码/语义"，全部 HTTP 绑定停在 phase 2 之前。
- **Phase-1（合同无关面，代码提交 7302cc1）**：dconfig（insightface provider + 9 个 MVP_D_FACE_SERVICE_* 键，严格解析，零 secret）；dtokenfile（仅 0600 普通文件，os.open+fstat 同 fd 防 TOCTOU，缺失/非普通/权限不符/空 → TokenFileError fail-closed，值绝不进异常/日志/repr）；dtransport（传输协议 + **通用** HTTP 语义分类——错误码名永不参与 + FakeTransport + SealedHttpTransport phase-2 缝）；dliveness（永不请求 require_liveness；supported=false/缺失/畸形≠失败；结论词表无 passed）；InsightFaceAdapter（5 操作 → FaceServiceNotBound ⊂ ProviderUnavailable → 既有可重试 DEPENDENCY_UNAVAILABLE，零静默成功、零 FaceDouble 回退；Aliyun 仍 ProviderNotActivated）；face 替身生产拒绝扩展到 production_environment_signals()。**零合同绑定** + 防绑定守卫测试（提前硬编码即挂）。
- **证据**：全量 pytest **608 passed exit 0**（459 基线+149 新增；fix lane 与 orchestrator 亲跑各一次，绑定同一内容）；face-service/contracts/*.java/migrations/handlers/fencing 文件 diff 全空；幂等/照片版本/processing_revision/租约/陈旧围栏零触碰；src 内 0 处 token 占位符。
- **Phase-2（waiting_dependency）**：所需输入与精确缺口见 .mvp-d-runtime/waiting-dependency-insightface.md §3 + insightface-contract-coordination.md（协调请求 S 节已同步）：①权威已提交 B face-service SHA+对应 Oracle 结论；②1:N search 完整合同（路径/请求/响应 schema、top_k/threshold 语义、候选/分数语义、错误码×状态×retryable、matched 判定归属、uncertain/ambiguous 区分信号、reliable_new 条件、分数跨 library_revision 语义、并发一致性）；③既有 quality/extract/verify/subjects/info schema 在权威 SHA 的终局性书面确认；④contracts 目录归属裁定（face-service 合同是否入 contracts/；payload-identity-enroll.json 在新裁定下地位）。解锁后：真实 HTTP 客户端（同 seam）、逐码错误映射、跨语言合同测试、1:N 分类策略落地、阈值校准、并发/幂等/租约丢失/旧照片版本迟到/enroll 对账/服务不可用/非法响应回归、活体接线。**最终流程=真实 Oracle 审查最终代码 SHA→修全部阻塞→复审→独立提交代码与报告→更新本文件与 D-oracle.md**（本节为进行中状态记录）。

## InsightFace 接入轮（2026-09-16/17）：phase-1+2 交付完成，最终代码 SHA `5e52cde`（Oracle 第三轮 PASS-with-notes ACCEPTED）【历史结论·截至第三轮，已被 `e006e6b` 取代】

> **历史标注（2026-09-17，report-only 最小修订）**：本节（至"现场验收限制/Oracle 记录"条目为止）所述"最终代码 SHA `5e52cde`/ACCEPTED 为 FINAL"为**截至 Oracle 第三轮的历史结论**；其后 NO_FACE 重拍语义轮（Oracle 第四轮 PASS、零发现、blockingFindings=[]）已以 `e006e6b` 取代。现行最终代码 SHA 以本文末节"NO_FACE 重拍语义轮 + 成功路径 live"为准，Oracle 记录以 D-oracle.md 第四轮为准。本节其余内容（裁定/交付链/绑定面/证据/现场验收限制）仍为有效事实记录，除本标注外原文保留。

- **裁定与基线**：用户最终裁定=Worker 保留身份协调职责（assessment.analyze 的 quality/same_person/search_1n + member_id/identity_result 决策；identity.enroll 的 register/query/face_subject_ref/member 绑定），Java 人脸职责不变；此前"停用 Worker 人脸职责"计划撤销（identity-ownership-audit §5 作废，仅作现状审计，锚点经核在基线全匹配）。基线 8d7aabb（dev 3e29722 clean 合入；B face-service 最终代码 bec9eb97=B-oracle r31 reviewedCommit，PASS-with-notes+整合许可）。**冻结合同**=backend/handoffs/B-face-service-worker-contract.md（373 行：13 端点、search 三值 decision（ambiguous 以 uncertain+ambiguous=true 表达）、18 错误码双向锁、幂等登记+对账矩阵、阈值全部服务端固定、liveness 恒 supported=false）。4a5e52d 经核实为 OSS 日志卫生提交、非 face-service 合同 SHA（已排除）。
- **交付链**（全部独立任务级提交，未 push/未合并 dev/未部署）：`7302cc1` phase-1（合同无关面：配置骨架、0600 token 安全读取（TOCTOU-safe、fail-closed、值零外泄）、通用传输 seam、liveness 纯政策、not-bound fail-closed 骨架、149 纯单元测试；608 绿）→ `d51dfe0` phase-2（五操作真实 HTTP 绑定；Oracle 第一轮 FAIL：2 BLOCKER+2 IMPORTANT）→ `99792a5`（四发现闭合；Oracle 第二轮：原探针全闭合+过约束审计干净+零回归，但 FAIL：新 BLOCKER query 未验 HTTP 状态+新 IMPORTANT register namespace/三态语义）→ **`5e52cde`**（query 200 状态门+namespace 精确等值+201/200 状态三元组；**Oracle 第三轮 PASS-with-notes，ACCEPTED 为 FINAL phase-2 increment code SHA**，blockingFindings=[]）。报告提交=本轮（report-only）。
- **最终绑定面**（InsightFaceAdapter+StdlibHttpTransport+dconfig，恰三文件+两测试文件）：quality=逐视角 POST /v1/quality（HTTP 前入参完整性校验；不合格视角规范序元组；任一失败→ProviderUnavailable、鉴权→ProviderConfigError，绝不把失败当合格）；same_person=front 锚定两两 /v1/compare（两次必执行、不发 threshold——服务端固定）；search_1n=front→POST /v1/namespaces/{ns}/search（不发 top_k；decision∈{matched,uncertain,reliable_new} 直通为 classification；十个 §4.3 冻结键严格校验+matched 专有 subject_id/数值 similarity+非 matched 禁带 subject_id；违例→ProviderUnavailable 绝不伪造；4xx 参数/鉴权→ProviderConfigError、5xx/429/504/MODEL_*/STORE/超时→ProviderUnavailable）；register_person=**AUTO_ENROLL 门先行**（MVP_D_FACE_SERVICE_AUTO_ENROLL 严格布尔**默认 false**→ProviderNotActivated 零网络——合同 §4.2 裁定 2+后端详细设计:663 PoC 门：reliable_new 绝不直接触发真实自动登记），开门后仅收 200/201、12 键冻结形状（api.py:726-742 逐键取证）、subject_id==entity_id、namespace 精确等值、201⇔created∧¬replayed / 200⇔¬created∧replayed（store.py 真值核验：Worker 永不发 on_exists→唯一可达 200=幂等重放），任何失配→unknown→强制对账；query_registration=status==200 门（B 路由恒 200）+七键+subject_id/correlation_id/provider_request_id 三标识精确等值→registered，传输/5xx/无法解析→unknown（仅客户端态），404 NAMESPACE_NOT_FOUND→not_found（§6.2 确定性否定，与 unknown 同走 ENROLLMENT_RECONCILE_PENDING 无死锁）。18 码表 FACE_ERROR_SPECS 双向锁（测试以合同字面清单断言、不 import B errors.py；INTERNAL_ERROR=(500,True) 经 Oracle 对 B 真值裁定正确）。liveness：永不发送 require_liveness（dliveness 政策参数为空）、supported=false 永不判败、绝不产出 passed、相似度≠现场性（docstring 明示"照片比对无防翻拍"）。脱敏：token/图片字节/embedding/namespace/subject_id/correlation_id 取值绝不进日志/异常/repr（含链式 __cause__/__context__ 扫描）；details 不读不记。FaceDouble 生产拒绝扩展到 production_environment_signals()。Aliyun 仍 ProviderNotActivated。冻结面恒 diff 0：两 handler、dfence/denqueue/resolve/constants/dtokenfile/dliveness、FaceDouble/Aliyun 体、backend/face-service、contracts、handoffs、全部 Java、migrations；幂等/照片版本/processing_revision/租约/陈旧围栏零触碰。
- **证据**：每候选全量 pytest 由 orchestrator 亲跑并绑定提交内容：d51dfe0=673、99792a5=745、**5e52cde=762 passed exit 0**（608 继承基线+154 insightface：phase1 81+phase2 222 收集中新增部分）；lane 自跑同数。受控联调=真实 StdlibHttpTransport 端到端对测试自建本地 stub（合成图、占位 0600 token、临时回环端口、门禁/revision/泄漏断言；**stub≠B face-service，已如实标注**）；真服务 FakeModel 无法纯 env 激活（create_app 需显式 model= 注入、无 FAKE* 开关）——未绕过，真服务活体证据列为协调待裁定。
- **现场验收限制（持续披露）**：①阈值全部未标定（服务端 0.60/0.10/0.05/0.40 保守占位）→ 标定+PoC+活体门禁验证前 AUTO_ENROLL 必须保持关闭（开门需协调授权）；②无活体能力→人脸结论=照片比对、无防翻拍；③已部署 face-service 实例须由根重新部署（subjects 三列+唯一索引原地升级在重启时发生；deploy.sh 含 ExecMainPID 断言+/ready 门禁）；④生产部署必须保持 FACE_SVC_REGISTER_ON_EXISTS=conflict（Oracle 第三轮限定：全局 overwrite 配置可产生 200/created=false/replayed=false——D 侧方向安全（unknown→对账确认，overwrite 已存本请求标识）但属非合同形态）；⑤根侧须监控 subject count/library_revision，库异常清空时停用自动登记（B r31 §11.7 生产前提；PG 对账只防重复不防批量新人候选）；⑥S5：ProviderConfigError 经冻结 handler 表现为有界可重试 HANDLER_ERROR→干净终态（fail-closed 保持；若运营需要立即终态需协调 handler 合同变更，未授权本轮改）；⑦Oracle SUGGESTION 1（providers.py:920-923 可达性注释应提及全局 overwrite 配置）与 SUGGESTION 2（+17 测试中部分为正例非判别性负例——措辞精度）登记在案，不为 SUGGESTION 重开已接受 SHA；⑧/v1/verify 客户端 threshold 不对称=B 侧待总协调裁定项（本适配器不使用 verify）。
- **Oracle 记录**：D-oracle.md 本轮三节（三轮实际调用、同一审查会话、reviewedCommit 均绑定真实 SHA、探针审查者亲跑）。

## InsightFace 8010 同机活体冒烟轮（2026-09-17）：真实链路+失败/重试映射实证、零写入（HTTP 旁证）；质量重拍门未 live 触发（如实记录）

- **裁定与方式**：跨主机 token 传递被自动审批拒绝（未以任何方式重试）；改为 dev.ai-skin 同机联调——0700 临时目录、仅传 dev 已提交 Worker **stdlib 最小链**（providers/dconfig/dtokenfile/dtransport/dliveness/constants/weijing_mapping/media.storage/logging_setup+空 shim 替代重 __init__；第三方 import 主动阻断验证）+驱动脚本+3 张纯 stdlib 合成**非人脸** PNG；远端进程经 dtokenfile 同机读取既有 0600 token 文件（值零输出/零拷贝、不读整份 env）；URL=http://10.3.6.163:8010、隔离 namespace=d-insightface-smoke-20260917、AUTO_ENROLL=false（驱动内断言 cfg 为 False）；远端 Python 3.12.3。
- **部署事实**：真实 buffalo_l@insightface-0.7.3（非 FakeModel）、library_revision=0、liveness supported=false、uptime 连续 1094.547→1099.204s（无重启）。
- **Worker 实测**（真实 InsightFaceAdapter+StdlibHttpTransport；服务级冒烟与 Worker 调用分开记录，全部响应带独立服务端 request_id）：quality×2、same_person×2、search_1n×1 → 真实 **400 NO_FACE → ProviderUnavailable**（合同 §8"任一失败→可重试、绝不把失败当合格"live 实证；search 的 NO_FACE 非 4xx 参数/鉴权类→ProviderUnavailable=既有记录的偏差决策，live 一致）；register_person 门关 → **ProviderNotActivated 且零 HTTP**（自动登记门 live 实证）；query_registration → **404 NAMESPACE_NOT_FOUND → not_found**（§6.2 确定性否定路径首次 live 实证）。
- **质量门禁取证**（orchestrator 对已提交源码亲核）：quality.py:125-129 `min_acceptable` 恒由 det_score/blur/bbox_area_ratio 三阈值真实计算（enforce 不改变该计算）；api.py:710-711 `quality_enforce` 仅控制服务端**登记**硬拒绝（config.py:140 默认 false；总协调确认 8010 有效值=false）。⇒ Worker `min_acceptable→needs_retake` 聚合**理论上可 live 触发**；但真实 buffalo_l 对三张合成非人脸图全部 NO_FACE → **重拍门本轮未 live 触发，仍为单元验证（762 套件）**；同理 401→ProviderConfigError 仅单元验证（token 有效）。**不宣称 live 质量门禁已启用。**
- **零写入证据（HTTP 旁证，非全库证明）**：library_revision 0→0；smoke ns 前后均 404（从未创建）；register_http_issued=0；uptime 连续（无重启）；事后独立复核 /ready revision=0 + 鉴权 info 404。服务无全局 namespace/subject 清单端点 ⇒ 以上为 D 侧可得的最强 **HTTP 旁证**；**权威全库确认=B 的独立 SQLite 复核：已完成，namespaces=0/subjects=0/library_revision=0（B 测后独立核对，总协调 2026-09-17 通报确认）**——与本节 HTTP 旁证一致；在该结果返回之前本节旁证不构成全库证明（方法论限定保留）。
- **卫生与清理**：token 值/图片字节/整份 env 零进入日志、证据与命令行（evidence.md 脱敏扫描 0 命中；远端仅读 token 文件权限位=600）；远端临时目录已删且经 lane+orchestrator 双侧复核不存在；本地证据仅 .mvp-d-runtime/live-8010/（git-ignored：driver.py/gen_images.py/images/stage/evidence.md）；repo 零代码改动，本提交 report-only。
- **残留限制**：无 live 质量/重拍样本（合成图设计上非人脸，属预期）；如需 live 触发重拍门需授权可合成人脸样本或协调安排标定样本（真人照片仍禁止）。

## NO_FACE 重拍语义轮 + 成功路径 live（2026-09-17）：最终代码 SHA `e006e6b`（取代 5e52cde；Oracle 第四轮 PASS 零发现）

- **缺陷确认与决策**（总协调 §609 裁定触发）：状态机取证——assessment_analyze.py:260-267 将 quality 的 ProviderUnavailable 映射为可重试 DEPENDENCY_UNAVAILABLE（有界重试→终态 FAILED），而 :268-274 才是确定性照片问题的正解路径（QualityResult≠accepted→_retake_result QUALITY_REJECTED+required_views）；live 证据（evidence.md）证实真实 buffalo_l 对无脸照片恒 400 NO_FACE。⇒ NO_FACE/MULTI_FACES_AMBIGUOUS/IMAGE_DECODE_FAILED 在 quality 阶段=确定性图像内容问题（同字节恒同 400），业务正解=needs_retake 补拍而非重试服务；与真实网络/5xx 瞬态故障严格区分。
- **合同 §8 修订案文（明确标识，报总协调裁定；B 冻结合同文件未修改）**：quality 行修订为——确定性图像内容 400（NO_FACE/MULTI_FACES_AMBIGUOUS/IMAGE_DECODE_FAILED，按 CODE 绑定）⇒ 该视角计不合格（等价 min_acceptable=false）⇒ QualityResult("needs_retake", 不合格视角规范序)；其它任何失败仍抛：传输/超时/5xx/429/504/其它 4xx/畸形 2xx⇒ProviderUnavailable（可重试），鉴权⇒ProviderConfigError；**瞬态支配**：任一视角抛出即中止整个 quality()——任何视角状态未知时绝不得出 needs_retake，瞬态消退后重试收敛；全部 min_acceptable=true⇒accepted；**绝不把失败当合格**；needs_retake 由 assessment_analyze.py:268-274 消费为 QUALITY_REJECTED 补拍。裁定注记（总协调 2026-09-17）：三码为业务语义（驱动补拍），区别于瞬态服务故障；细化仅限 quality 阶段——same_person/search_1n 映射不变。
- **最小修复**（`e006e6b`，恰 2 文件 +181/−5）：_QUALITY_CONTENT_FAILURE_CODES（providers.py:677-679，含裁定/状态机/live 证据引证注释）；ruling branch（:756-771）=鉴权优先→内容码 return False→其它照抛；quality() 聚合不变式（:738-749，规范序、零失败才 accepted）；same_person/search_1n 零改动+scope 守卫测试钉死；零 handler/dconfig/dtransport 改动；+16 判别测试（临时变异 `if False:` 使 10/12 对 pre-fix 必红；sha256 逐字节还原核验、零 MUTANT 残留）；1 个既有测试语义重划（NO_FACE/MULTI 参数移出 unavailable 矩阵，瞬态参数保留）。
- **Oracle 第四轮**（同审查会话 ora-1）：**PASS@e006e6b=ACCEPTED 为新最终 phase-2 代码 SHA（取代 5e52cde）**；七焦点全 COMPLIANT（语义/瞬态支配沿 :585-601 有界重试核验收敛/scope 纪律含 _bounded_views 与 requiredViews 通道一致/业务结果/测试判别力+变异零残留/5e52cde 已认可面零回归/§8 修订案文准确）；blockingFindings=[]；**零发现**（本谱系最干净一轮）；八对抗探针全阻断（accepted 逃逸/部分 retake/鉴权与瞬态支配序/双 scope 泄漏/required_views 排序规范化/静默跳视角）；按 CODE 绑定裁定恰当（三码恒 400 由冻结码表保证、不依赖中间状态、auth 仍优先）。
- **成功路径 live**（B 提供合成人脸样本，仅远端原地读取、零复制、digest 身份 sha256=a1503c48…；e006e6b 移植字节等价核验+第三方 import 阻断复验）：quality(B 样本×3 视角)→**accepted,() 首次 live**（200/min_acceptable=true/face_count=1 ×3）；same_person(同图)→**True 首次 live**（front 锚定两次 compare 均 matched=true）；search_1n(隔离 ns 不存在)→**reliable_new+reasons=[empty_library]+policy_version=search-v2 首次 live**（合同 §4.2 裁定 1 首个 live 实证；subject_count=0、library_revision=0、无 subject_id/similarity；**search 未创建 namespace**——紧随其后 info 404）；**needs_retake live 实证（e006e6b 修复）**：混合（front=B 样本 200，left/right=无脸图 400 NO_FACE）→needs_retake("left","right")，全无脸→needs_retake("front","left","right")，均无异常——与第一轮 ProviderUnavailable 失败路径记录明确区分；register 门关→ProviderNotActivated 零 HTTP 复证；query→not_found（404 NAMESPACE_NOT_FOUND）。真实推理延迟 2.6-21.4s 全部低于 35s 读超时（无客户端误超时）。
- **零写入证据（HTTP 旁证，非全库证明）+清理**（orchestrator 独立 ssh 复核+/ready 复测一致）：library_revision 0→0；smoke ns 404（pre/紧随 search/post）；uptime 连续 2698.432→2811.006s（无重启）；register_http_issued=0、传输记录无 POST subjects；自有临时目录删除验证 gone；B 样本文件已删、专用目录 d-20260917.4WLQL6 删前核验（0700/0600 bool:bool、remaining_entries=0）后删除；父目录 .face-service-smoke 未触碰（原本仅含该样本，现为空——如实记录）；token 文件 600 bool:bool 未变。**限定**：revision 0→0、ns 三时点 404、uptime 连续、register_http=0 均为 HTTP 旁证，本身不构成全库证明——权威全库确认=B 的独立 SQLite 复核：**已完成，namespaces=0/subjects=0/library_revision=0**（B 测后独立核对，总协调 2026-09-17 通报确认），与本轮 HTTP 旁证一致，零写入全库结论闭环。
- **证据与卫生**：.mvp-d-runtime/live-8010/evidence-success.md（新文件 266 行脱敏 JSON：仅状态/码/判定/分类/布尔/request_id/耗时+安全标量；第一轮 evidence.md 原样保留）；脱敏扫描 token 模式 0 命中、唯一 64-hex=获准的样本 sha256 摘要；token 值/图片字节/标识取值零进入任何输出。
- **未验收边界（更新）**：401→ProviderConfigError 仍单元验证（token 有效未触发）；register/query 的真实登记成功路径仍单元验证+门关 live 实证（AUTO_ENROLL=false，未授权登记）；真人照片 live 验证仍禁止（B 合成样本非真人脸）；阈值未标定/无活体/部署与监控前提等既有现场验收限制不变（见上一节）；**B 的独立 SQLite 全库复核已完成：namespaces=0/subjects=0/library_revision=0（总协调 2026-09-17 确认）——零写入全库结论闭环；B 已正式同意 D 的业务裁定（§8 quality 行修订以独立 report-only 提交记录）**。
- 提交：代码 `e006e6b`（独立提交）；报告=本提交（report-only）。

## V3 测肤三组发布增量（2026-09-17）：最终代码 SHA `e133461`（Oracle rebind PASS，取代 9f96f10）——尚未现场接入真实算法

- **授权与边界**：总协调批准的本地增量；仅在 D feature 工作树实施（基线 f3c29f0=本地 dev）；**未合并本地 dev、未 push（origin/dev 推送为自动审批阻断，未重试/未绕过）、未部署、未改 B Java SSE 工作树**；零真人/密钥数据；`.mvp-d-runtime/v3-probe-input/` 用户样例=仅合同/mock 数据（**非线上算法证据**），文档内指令未执行。
- **B 最终消费形状（给定，未重设计）**：T05 report_payload 顶层可选 `pores`/`spots`/`surface_gloss` **all-or-none**；每组 {score,severity,name,regions[]}；regions 项 {region,name,score,severity}；Java 只白名单读三组、AI body=恰三个顶层对象；缺省保持缺项→Java 调 AI 前安全拒绝。
- **V3 语义（绑定；字段说明 data-only）**：score 0..100 **高分更好**、可 null（null=缺测≠0，绝不凑零）；severity 词表 `未见明显/轻度/中度/较明显/显著` 可 null；**画面左右原样逐字保留**——不做 F/L/R 或本人左右转换、不凑四区、不从旧 moisture/oiliness/smoothness 推算；未误用旧 shuiguang_cloud_v1 raw_detection 映射（llm_rag/weijing_mapping 路径零触碰）。
- **实现**（首轮 6 文件+修复轮 3 文件，纯增）：SkinAnalysisResult.v3_groups 可选尾字段（位置构造兼容，仓内无不兼容调用）；SkinDouble 构造期+逐调用双深拷贝隔离；**MVP_D_SKIN_V3_MOCK 严格布尔默认 false**（仅 dev/test 替身携带 mock 三组；mock 标记=model_version `skin-v3-mock@0` 经 model_info 落地、不新增 payload 键；生产信号+true→既有 assert_no_double_injection_in_production 拒绝启动；hold 优先于 mock 不掩盖故障语义）；dskin_mock.py=用户样例 data-only 派生（保留画面左/右名+null 缺测区+边界 100.0，docstring 明示非算法证据）；AliyunSkinAdapter 仍 ProviderNotActivated（绝不冒充真实算法）。assessment.analyze 发布前严校 `_validate_v3_skin_groups`（:675-785，:304 于既有 _ContractViolation 作用域内调用）：顶层**恰**三键集合相等/每组与每区域闭合白名单且全必填/score null 或 0..100（**比较不经 float 转换**——任意精度整数精确比较，巨大整数/±inf/nan/bool/字符串一律判违约、绝不逃逸为瞬时依赖错误）/severity 冻结词表/region 组内唯一/name 逐字；违例→既有终态 **PROVIDER_CONTRACT_VIOLATION**（零发布、零部分发布、零静默丢弃、零凑零）；通过→规范化重建深拷贝，发布在既有 fenced _publish 内**恰三键**合并顶层（:448-452）；缺省→payload 原样（无 null 键无零）。
- **contracts 核查（只读，零修改）**：**无缺口**——无任何已提交 schema 约束 report_payload 顶层键（decisions-notes.md:146 仅"可空、有值必须带版本"；openapi SkinReportView 明示"摘要结构未冻结"且 additionalProperties 开放）。
- **Java 投影不泄（D 自有 web-java，仅测试改动）**：SkinReportService.project() 显式字段白名单**结构性闭合**（:142-156）、validateStructure 不检查未知顶层键（:174-194）；新增针对 IT `v3GroupsNeverLeakIntoProjection` 种入**携带三组**的冻结 payload，断言 APP full/APP brief/云台 brief 原始 body 均不含带引号的 "pores"/"spots"/"surface_gloss"（AssessmentReportIT 11/0/0/0 BUILD SUCCESS，仅跑该类，无关套件未重跑）。
- **AI 合同兼容性差异（精确记录、未决、绝无自行转换；待总协调/B/AI 侧裁定）**：B 的"AI body=恰三对象"与**已实证**的 weijing assess 合同不兼容——①容器形状：AI 需 `regions` **字典**（键 F/C/L/R、各含 O/P/D），三对象体无该字典→已实证 **400 AI_REQUEST_INVALID"检测报告 JSON 缺少四区评分 regions"**（Probe A 逐字样例）；完整性非阻塞因素（F/C-only 探针→200 partial、parsing_notes 缺 L/R），**容器形状才是**；②条目词表：V3 三组≠O/P/D（仅 spots↔P 对应；surface_gloss↔O 近似=v2_oiliness_tendency 含油光+卟啉、非纯油光；pores 无对应；D 无 V3 来源）；③区域帧：V3 细粒度 ID+**画面左右**命名 vs AI 需恰 F/C/L/R 且其 L/R 帧**无定义**（**N.5 OPEN** 需一次书面裁定）；V3→F/C/L/R 聚合=**未授权**的生产映射（D 不自转换）；④正向：分数方向一致（V3 高分=好 = AI 高分=好，live 已证）→**无需数值变换**；⑤信封（N.4）：V3 精简 payload 缺 schema_version/task_id/views，捕获/转发需 N.4 裁定。**D 范围止于 T05 发布。**
- **Oracle 谱系（同一审查会话，两轮实调）**：第一轮 @`9f96f10` **PASS-with-notes ACCEPTED**（九焦点全 COMPLIANT；blockingFindings=[]；15 探针 14 阻断+1 记录；IMPORTANT=巨大整数 float() OverflowError→误分类瞬时 DEPENDENCY_UNAVAILABLE（fail-closed 有界零发布，但与"违例穷尽终态"声明矛盾）；SUGGESTION=构造入参未防御拷贝）→修复轮（双点位去 float 转直接任意精度比较+构造深拷贝+11 测试；判别证明：临时回退 float() 形态**恰 3 失败**复现缺陷、sha256 还原零残留；inf/nan 如实标注非判别守卫）→**rebind @`e133461`：PASS，ACCEPTED 为 FINAL V3-increment code SHA（取代 9f96f10）**；两发现 CLOSED（探针亲跑：10**10000/±inf/nan/True/"50"/complex→违约；None 保留；双向拷贝隔离）；四偏差逐项裁定可接受（Py3.12 4300 位 int→str 限制仅 mock 构造路径、真实 provider 走校验器任意精度精确处理/label 参数化/in-process stub=JSON 无法携带非有限值的恰当选择/守卫如实披露）；零回归全确认（dconfig/dskin_mock 字节未变）；零新发现。
- **证据**：worker 全量每候选 orchestrator 亲跑绑定——9f96f10=821、**e133461=832 passed exit 0**（778 基线+54）；Java 针对 11/0/0/0（9f96f10 时跑；rebind 轮 Java 零 delta，证据沿用有效）；无关套件未重跑。
- **边界（尚未现场接入真实算法）**：真实测肤算法**未接入**——AliyunSkinAdapter 未实现/未激活；V3 三组当前仅能来自显式 mock 开关（默认关）后的 dev/test 替身（带 mock 标记）；生产 fail-closed 零注入；真实算法 provider 接入并经协调标定前，不存在 V3 生产发布路径；AI 侧对三组体的消费与已实证合同存在上述未决差异（容器形状/词表/N.5 帧/N.4 信封）。
- 提交：代码 `9f96f10` + 修复 `e133461`（独立提交）；报告=本提交（report-only）。

## V3 校验收紧轮（2026-09-18）：最终代码 SHA `959af5d`（取代 e133461；Oracle PASS 零发现）

- **基线**：监督者已将本地 dev 快进同步入 feature（链 …→9983c1d→dev 合入（含 B 的 ReportNarration SSE 增量 31 文件）→29e7287→959af5d）；治理文档（AGENTS.md/doc README/COMMON）零 delta。仅本地——未合并 dev、未 push、未部署、未改 B 代码/其它工作树。
- **裁定与范围（总协调 §688）**：仅收紧 D Worker V3 三组预发布校验——**拒绝空 regions 列表**与**空白字符构成的 group.name/region.name/region.region**，与 B 已集成 SSE extractor 的拒绝语义对齐（取证亲读：ReportNarrationScoreExtractor.java:151（isNull/!isArray/isEmpty→invalid）与 :174-176（过滤后空→invalid）、readRequiredText:239（!isTextual||asText().isBlank()→invalid）施于 :133/:164/:165、接受值不 trim；ReportNarrationScores.java:34-41/:51-57 记录级复核）；其它已验收契约与 mock 默认关**不变**；最小测试（未跑全量/重型模型）；**真实 dermavision 适配明令未做**。
- **实现**（恰 2 文件 +108/−9：validator +24/−9、测试 +84）：三处文本检查由真值升级为非空白（`.strip()` 非空；**仅拒绝、绝不 trim/规范化**——与 B 的 asText() 不 trim 一致）；regions 非空数组守卫（既有 isinstance-list 检查之后）；违例仍走 _ContractViolation→终态 PROVIDER_CONTRACT_VIOLATION（零发布）。**方向安全实证**（Oracle 亲验）：JavaWhitespace−PythonStrip=∅（跨已安装运行时）；Python 额外剥 U+0085/U+00A0/U+2007/U+202F=仅 D 更严（NBSP 类 D 拒 B 收）⇒ D 放行值必可被 B 消费；D 既有更严处（未知键拒绝 vs B drop+warn、非有限/巨大整数拒绝）保持**零放宽**。其余行为字节级不变：null≠0、非 float 0..100 精确比较、severity 词表、region 唯一、all-or-none 恰三键、闭合白名单、规范化深拷贝、fenced 恰三键发布合并；dskin_mock/dconfig/providers **字节未变**且 mock 数据未改即过新规。
- **测试**（最小验证=仅定向文件；rg 复核无其它文件引用 v3_groups）：tests/test_skin_v3_groups.py +16（54→**70 passed exit 0**，orchestrator 亲跑绑定）：空 regions×3 组；三文本位×3 空白形态（" "/制表换行/全角 U+3000）；publish 级空 regions+空白组名→终态 PROVIDER_CONTRACT_VIOLATION（retryable=False、report_payload/report_id 缺位）；内部与首尾空白（"额 部"/" 额 部 "）**逐字节 verbatim 保留**。判别变异：回退真值检查+移除空守卫→恰 14 判别用例失败（2 个 verbatim 保留用例如实标注非判别守卫）；sha256 逐字节还原、零残留。
- **Oracle**（同一审查会话，有界轮）：**PASS@`959af5d`=ACCEPTED 为本收紧增量最终代码 SHA**；六焦点全 COMPLIANT（对齐保真/方向安全反向缺口搜索/零松弛回归/测试判别力/scope 纪律含禁 dermavision 适配/引证准确性）；blockingFindings=[]；**零发现（全严重级）**；14 探针全阻断。事实更正（审查者核出）：validator 实际插入 24 行（审查指令中 33 为 diffstat 变更行数；合计 +108/−9 不变；仓库工件无该误数）。
- **残留真实适配缺项（未做、不编造）**：可见最新 origin/dev `787a500` 的真实 dermavision API **只接受单图或 RGB/PP/CP/UV 四图、返回 11 模块两种评分**，**不提供**用户给的 pores/spots/surface_gloss、9/8/9 regions、高分更好的精简 V3 API；**三视角调用方式与分数映射未定**——真实适配需总协调/算法侧裁定后另起增量，D 零猜测实现、零伪造映射。集成由总协调负责。
- 提交：代码 `959af5d`（独立）；报告=本提交（report-only）。V3 谱系 Oracle 累计：9f96f10 PASS-with-notes→e133461 PASS（历史 FINAL）→**959af5d PASS（现最终代码 SHA）**。

## shuiguang 真实测肤适配器增量（2026-09-18）：最终代码 SHA `18bf17e`（Oracle 三轮：FAIL→FAIL→PASS-with-notes ACCEPTED）——不可激活态交付，激活被 ops 前提把关

- **目标纠正与授权（总协调 §708）**：真实算法仓库=**shuiguang-test**（远端 dev d7c84c18，监督者只读核实），绝非 dermavision；旧 `shuiguang_cloud_v1`=PLAN 路径遗留 schema，零触碰。仅本地实施（基线 ceb0090=dev）；未合并 dev/未 push/未部署/未改 B 专属源码；零真人照片/零密钥；无 GPU/重型模型；无全量套件。
- **绑定合同（监督者从 d7c84c18 核实，逐字绑定零猜测）**：POST /api/score-jobs {task_id 1..64 [A-Za-z0-9_-] 同 ID 同输入幂等, images 恰三 {image_id, path 相对 SHUIGUANG_INPUT_ROOT 的 JPEG/PNG}, mirrored:false}→202 {task_id,queue_id,status:queued,score_source}=仅排队；GET /{queue_id}：pending/started/progress→轮询；成功→**直接恰三组**（V3 形状）；失败→{status:failed,error{code,message}}，Celery 异常=WORKER_FAILED。算法自动辨乱序三视图仅评正面；高分更好；左右=画面。**真实样例 9/6/9 区（spots 6）原样消费、绝不补造旧 8 区**；诊断 scores 包裹层未绑定。仅收共享挂载路径（无 OSS ID/字节）。
- **实现**（ShuiguangSkinAdapter，dshared/dshuiguang.py 新模块，stdlib-only，可注入 request_fn/sleep/clock/caps）：内容派生 task_id（"sg-"+sha256 长度前缀 160-bit 截断=**概率性抗碰撞**，同字节幂等）；暂存 {root}/{task_id}/{view}.{ext} 临时文件+os.replace 原子、**目录 0700/文件 0600 免 umask**（严格八进制钮可配，默认从紧）；两段超时+有界读 1MB；202 严校（task_id 回显/queue_id/status/score_source）；**有界轮询**+预算超时→可重试（同 task_id 幂等重提恢复同 queue 条目）；成功体=dv3 **单一事实源**严校（与 959af5d 验收版逐规则等价）；结果 metrics=[] 恰空、result_images=[]、conclusion/description=""——**零虚构**；model_version="shuiguang:"+消毒 score_source（仅真实响应字段）。失败分类矩阵：SCORING_REFERENCE_NOT_READY→立即终态独立码（绝不伪造分数）/WORKER_FAILED+未知消毒码→有界可重试/畸形→可重试/成功体违约→终态。激活门：缺 BASE_URL/INPUT_ROOT 或根不可写→ProviderConfigError 仅键名；resolve.skin_port_for 不再吞配置错（镜像 PLAN 先例）→handler 立即 fenced 终态 **SKIN_PROVIDER_CONFIG**（1 次不重试）；**V3-only 报告路径仅当且仅当显式 provider=shuiguang**（metrics 恰[]/images 空/三组必填，违者终态）；其它 provider 行为字节不变；默认 double、生产 double/mock 拒绝不变。
- **暂存生命周期（Oracle 两轮实证后定稿）**：**禁一切推断式补偿删除**（handler 出口/租约丢失/代次作废/最终尝试耗尽均不删——round-2 探针实证：基于可变当前照片推断会**删掉新活跃 job 的暂存**且漏清旧孤儿）；保留的适配器自清理仅限**成功与适配器级确定性终态**（依据 API 幂等：同内容=同 task_id=同 queue 条目，结果对所有同内容调用者共享）；可重试保留文件；_stage 失败仅删本次追踪临时文件；清理失败**可观测**（rmtree 不 ignore_errors→False→键名告警零值泄漏）。**三类孤儿（重试耗尽/照片版本换代/硬崩溃）显式延后至"激活阻断的 ops 孤儿合同"**（授权清扫器或经协调的持久暂存所有权记录）——激活本已被共享挂载+ops 裁定把关。
- **Oracle 谱系（同一审查会话，三轮实调）**：R1@9d8aba1 **FAIL**（2B+2I+1S：非崩溃永久孤儿敏感照片（最终耗尽/stale 取代/部分暂存）+暂存异常泄漏根/task 路径（探针实证 FileExistsError 全文含路径存活于 JobFailed 链）+umask 权限 0775/0664+DConfig repr 泄漏+碰撞措辞；合同保真/激活/V3-only/分类矩阵 COMPLIANT）→修复轮 367bd14（B2/I3/I4/S5 闭合确认）R2 **FAIL**（关键探针 BLOCK：stale 清理从当前照片版本推断→实证 old(A) 残留且 current(B) 被删=破坏性；abort=租约丢失风险；最终 attempt 在 fenced 提交前；新 IMPORTANT 清理失败静默）→方向纠正轮 18bf17e（按 Oracle 指定替代方案：移除全部推断删除+孤儿延后 ops 合同+告警可观测）R3 **PASS-with-notes，ACCEPTED 为最终 shuiguang 增量代码 SHA**；blockingFindings=[]；focus-6 明裁"round-2 BLOCKER 的可接受解法"；11 探针（真实 A/B 取代双目录均存活/租约丢失保留/最终耗尽保留/清理失败告警零泄漏/成功与确定性终态自清理仍工作/移除残留 grep 零）。
- **R3 新 IMPORTANT（非阻塞，激活门前必须处置——已列激活前提）**：同内容并发下，一方成功自清理与另一方 `_stage` 进行中存在**本地窗口**（探针实证：共享目录被删→B 得消毒 ShuiguangStagingError；通常重试后幂等重放已完成条目，但末次预算尝试可能不必要终态）；远端 queue 安全（一旦有调用者观察到成功/确定性失败，共享条目不再需要文件）。激活前需三选一：跨进程按 task 串行化 stage/submit/self-clean、或 staging 对目录消失本地可恢复、或适配器自清理也延后到所有权感知的 ops 机制；并补确定性交错测试；docstring"所有权安全"措辞应限定为**远端 queue 安全**而非完全本地并发安全（登记在案，不重开已接受 SHA）。
- **severity 词表事实纠偏（§728，监督者只读核实）**：shuiguang-test@d7c84c18 `cloud_three/public_scores.py` `Severity=Literal["未见明显","轻度","中度","较明显","显著"]`+ScoreResponse model_validate，**与 D 冻结词表完全一致**（两组真实 Celery 样例同词表）→词表**不再是未验证缺口**；冻结校验保留为纵深防御。（9d8aba1 提交说明中"unverified"表述有误，历史不改写，以本节为准；AdminReportCommandService 引证已全清，Java 引证仅 SkinReportService:146-156/:178-183+DB V1__create_tables.sql:219-220。）
- **消费侧核验（只读）**：DB ck 仅要求 jsonb object+numeric schema_version→metrics=[]+三组过（测试实证发布）；Java SkinReportService 白名单投影结构性闭合（[] 与 "" 通过、三组永不外泄，9f96f10 轮 IT 已证）；B SSE extractor 仅要求非空 regions→**6 区原样消费**（B 的 ReportNarrationScoreExtractorTest 只读跑 15/0/0/0）。
- **证据**：定向 8 文件组每候选 orchestrator 亲跑绑定——9d8aba1=367、367bd14=376、**18bf17e=377 passed exit 0**（test_shuiguang_skin 60/60、test_skin_v3_groups 70/70；判别变异证明各轮 sha256 还原零残留）；按裁定无全量/重型。**真实活体未验证边界**：无部署地址/共享挂载/鉴权资料→**未激活、无活体烟测**（stub 级证据，已如实标注 stub≠真实服务）；算法回退默认开（reference_matches_new_preprocessing=false）边界如实报告；真实 9/6/9→AI（narration SSE）联调未做。
- **激活前提清单（全部满足前不得生产激活）**：①真实 base URL+共享挂载；②部署 UID/GID/ACL 裁定（默认 0700/0600 从紧，放宽需 ops 裁定）；③ops 孤儿合同（三类孤儿：授权清扫器或持久所有权记录）；④同内容 stage/clean 本地竞态机制三选一+交错测试（R3 IMPORTANT）；⑤真实 9/6/9→AI 联调验证。集成归总协调。
- 提交：代码 `9d8aba1`→`367bd14`→**`18bf17e`（最终）**；报告=本提交（report-only）。
