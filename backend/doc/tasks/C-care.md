# C：护理执行、次数与收尾

启动条件：A 验收合入 dev。负责 M4-A01～09 的 Java 实现，不写 Python 方案生成。

遵守详细设计：当前本人核验、当前任务、微晶能力、K<N 与双部分唯一约束原子准入；原控制端恢复；记录固定归属、稳定 ID/序号双去重、批次冲突整体回滚、K/执行汇总同事务更新；停止+完整水位收尾；闭合后迟到记录可入账但不重开。只做端侧控制的后端登记，不下发 start/stop。

验收真实 PG 并发准入、任务替换竞争、重复/异内容记录、K=9/10/11、记录插入后回滚、缺口收尾、旧成功响应重放不产生新核验有效性、授权撤销后最小对账。方案输入使用 A 契约夹具，与 D 联调；共享契约变更交总协调。

交付 backend/handoffs/C.md、测试和 feature 提交，不推送/合并。

## 逐项任务与 Oracle 门禁

见 [本包可勾选清单](panels/C.md)，清单定义保存在 checklists.json。交付前按 COMMON.md 的 Oracle 门禁执行，未获得真实 Oracle 结论不得视为完成。
