# E：全程验收与联调

立即启动，先准备验收，不等待 A 完成才开始。负责 backend/acceptance 及 backend/handoffs/E.md；不得修改 A 的实现、迁移、共享 Schema 或构建配置。

先基于当前 94 个场景和详细设计形成机器可执行验收计划/场景追踪矩阵、API 驱动测试框架、隔离测试数据/外部替身设计。无 A 基础时可以把测试标为 dependency_pending，不以全 skip 当通过，不另造第二个 Java/Python 工程或生产实现。

总协调提供 A 的已提交基线后，同步到本包，再执行基础验收：可启动/迁移/唯一约束/认证拒绝/Java-Python 契约和任务恢复。报告阻塞或通过证据给总协调，A 未通过不启动 B/C/D。

随后持续验收 B/C/D 的已集成提交与端到端链路。所有测试使用独立 DB/容器/端口；真实 PG 验证锁/唯一约束；区分替身通过与真实供应商/设备通过。不得读取整段 OpenCode 对话或只凭其说完成就通过。

未获授权不上传真实人脸、不购买/开通服务、不执行生产部署。报告包含：基线 SHA、命令、退出码、通过/失败/跳过及原因、阻塞包名。可复现缺陷交协调，不自行改别包。

每阶段提交验收产物与不超过 500 字摘要。等待依赖时不反复调用 Orchestrator，不为同一空进展重复消耗 token。

## 逐项任务与 Oracle 门禁

见 [本包可勾选清单](panels/E.md)，清单定义保存在 checklists.json。交付前按 COMMON.md 的 Oracle 门禁执行，未获得真实 Oracle 结论不得视为完成。
